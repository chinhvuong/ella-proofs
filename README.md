# ella-proofs

Public proof-of-work screenshots for the **private** [`chinhvuong/ella`](https://github.com/chinhvuong/ella) repo.

These images are hosted publicly so they render **inline** in issue/PR comments on the
private repo (GitHub's anonymous image proxy can't fetch a private repo's raw assets).
Organised by issue: `issue-N/<file>.png`. No secrets — UI screenshots only.
